---
name: Integration test common config
description: Common configuration for integration tests

variables:
  host: http://localhost:5000
  first_part: "nested"
  second_part: "{tavern.env_vars.SECOND_URL_PART}"
