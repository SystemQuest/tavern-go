---
# Common configuration for advanced tests
# Aligned with tavern-py commit 2a3725b - nested variables support
name: test includes
description: used for testing against local server

variables:
  service:
    proto: http
    host: localhost
    port: 5000
